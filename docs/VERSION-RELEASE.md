# Releasing a new plugin version to Freemius

**Moved 16/07/2026** — the plugin's build/CI/deploy (`freemius-deploy.yml`,
`build-zip.sh`, version bump steps) now live with the plugin's own source in
`Kieransaunders/divi5-generator` (private repo). See that repo's
`VERSION-RELEASE.md`.

This split out along with the rest of `plugin/divi5-generator/` — its
Pro-gating source can't sit in this public toolkit repo. See `docs/PRD.md`
§3.1 for why.
